import { DefaultTheme } from "react-native-paper";
// -------------------------------------------------

const theme = {
  ...DefaultTheme,
  roundness: 15,
  colors: {
    ...DefaultTheme.colors,
    primary: "#0072BC",
    accent: "#0AA1DD",
    white: "#FFFFFF",
    black: "black",
    grey: "rgb(180, 180, 184)",
    lightgrey: "rgb(199, 199, 204)",
    transparentGrey: "rgba(229,229,229,0.79)",
  },
};

// -------------------------------------------------

export default theme;
