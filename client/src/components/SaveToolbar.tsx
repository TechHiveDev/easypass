import React from "react";
import { TopToolbar, SaveButton } from "react-admin";
const SaveToolbar = () => {
  return (
    <TopToolbar>
      <SaveButton />
    </TopToolbar>
  );
};

export default SaveToolbar;
